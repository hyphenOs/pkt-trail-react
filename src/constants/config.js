const config = {
  dashboardConfig: {
    showSelectedDetails: true,
  },
  tableConfig: {
    packetWindowSize: 50,
    jumpSize: 10,
  },
  detailsConfig: {
    expanded: false,
  },
};

export default config;
